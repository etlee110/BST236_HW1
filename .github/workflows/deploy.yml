name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # Trigger deployment on pushes to the main branch

  workflow_dispatch:  # Allow manual deployment triggers

permissions:
  contents: read
  pages: write
  id-token: write  # Required for GitHub Pages deployment

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Setup GitHub Pages
      - name: Setup GitHub Pages
        uses: actions/configure-pages@v3

      # Step 3: Upload artifact for GitHub Pages (Corrected name)
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v2  # Ensure this is for GitHub Pages
        with:
          path: ./  # Deploy the root directory

      # Step 4: Deploy to GitHub Pages
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v2